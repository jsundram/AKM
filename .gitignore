.DS_Store

# test tooling (installed by the session-start hook / `npm install`)
node_modules/

# source archives (map + roster), not for the public repo
*.zip

# network diagram previews (bake in participant names — regenerate locally)
network-preview-*.svg
network-ego-*.svg
